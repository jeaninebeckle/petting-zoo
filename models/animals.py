from datetime import date

class Donkey():
	def __init__(self, name, species):
		self.name = name
		self.species = species
		self.date_added = date.today()
		self.walking = True

donkey = Donkey('Donnie Darko', 'donkey')

class Goat():
	def __init__(self, name, species):
		self.name = name
		self.species = species
		self.date_added = date.today()
		self.walking = True

goat = Goat('Gandalf the Grey', 'goat')

class Pig():
	def __init__(self, name, species):
		self.name = name
		self.species = species
		self.date_added = date.today()
		self.walking = True

pig = Pig('Peter Pan', 'pig')

class Llama():
	def __init__(self, name, species):
		self.name = name
		self.species = species
		self.date_added = date.today()
		self.walking = True
		
llama = Llama('Luna Lovegood', 'llama')

class Sheep():
	def __init__(self, name, species):
		self.name = name
		self.species = species
		self.date_added = date.today()
		self.walking = True
		
sheep = Sheep('Spongebob Squarepants', 'sheep')

class Copperhead():
	def __init__(self, name, species):
		self.name = name
		self.species = species
		self.date_added = date.today()
		self.slithering = True
		
copperhead = Copperhead('Captain Crunch', 'copperhead')

class Python():
	def __init__(self, name, species):
		self.name = name
		self.species = species
		self.date_added = date.today()
		self.slithering = True
		
python = Python('Peter Pettigrew', 'python')

class Cottonmouth():
	def __init__(self, name, species):
		self.name = name
		self.species = species
		self.date_added = date.today()
		self.slithering = True
		
cottonmouth = Cottonmouth('Chewbacca', 'cottonmouth')

class Kingsnake():
	def __init__(self, name, species):
		self.name = name
		self.species = species
		self.date_added = date.today()
		self.slithering = True
		
kingsnake = Kingsnake('Kirby', 'kingsnake')

class Anaconda():
	def __init__(self, name, species):
		self.name = name
		self.species = species
		self.date_added = date.today()
		self.slithering = True
		
anaconda = Anaconda('Aragorn', 'anaconda')

class Mallard():
	def __init__(self, name, species):
		self.name = name
		self.species = species
		self.date_added = date.today()
		self.swimming = True
		
mallard = Mallard('Minerva McGonagall', 'mallard')

class Goldfish():
	def __init__(self, name, species):
		self.name = name
		self.species = species
		self.date_added = date.today()
		self.swimming = True
		
goldfish = Goldfish('Godzilla', 'goldfish')

class Swan():
	def __init__(self, name, species):
		self.name = name
		self.species = species
		self.date_added = date.today()
		self.swimming = True
		
swan = Swan('Severus Snape', 'swan')

class Catfish():
	def __init__(self, name, species):
		self.name = name
		self.species = species
		self.date_added = date.today()
		self.swimming = True
		
catfish = Catfish('Cheshire Cat', 'catfish')

class Bass():
	def __init__(self, name, species):
		self.name = name
		self.species = species
		self.date_added = date.today()
		self.swimming = True
		
bass = Bass('Beetlejuice', 'bass')
