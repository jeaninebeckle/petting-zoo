from datetime import date

class Donkey():
	def __init__(self, name, species, date_added):
		self.name = name
		self.species = species
		self.date_added = date_added
		self.walking = True

donkey = Donkey('Donnie Darko', 'donkey', date.today())

class Goat():
	def __init__(self, name, species, date_added):
		self.name = name
		self.species = species
		self.date_added = date_added
		self.walking = True

goat = Goat('Gandalf the Grey', 'goat', date.today())

class Pig():
	def __init__(self, name, species, date_added):
		self.name = name
		self.species = species
		self.date_added = date_added
		self.walking = True

pig = Pig('Peter Pan', 'pig', date.today())

class Llama():
	def __init__(self, name, species, date_added):
		self.name = name
		self.species = species
		self.date_added = date_added
		self.walking = True
		
llama = Llama('Luna Lovegood', 'llama', date.today())

class Sheep():
	def __init__(self, name, species, date_added):
		self.name = name
		self.species = species
		self.date_added = date_added
		self.walking = True
		
sheep = Sheep('Spongebob Squarepants', 'sheep', date.today())

class Copperhead():
	def __init__(self, name, species, date_added):
		self.name = name
		self.species = species
		self.date_added = date_added
		self.slithering = True
		
copperhead = Copperhead('Captain Crunch', 'copperhead', date.today())

class Python():
	def __init__(self, name, species, date_added):
		self.name = name
		self.species = species
		self.date_added = date_added
		self.slithering = True
		
python = Python('Peter Pettigrew', 'python', date.today())

class Cottonmouth():
	def __init__(self, name, species, date_added):
		self.name = name
		self.species = species
		self.date_added = date_added
		self.slithering = True
		
cottonmouth = Cottonmouth('Chewbacca', 'cottonmouth', date.today())

class Kingsnake():
	def __init__(self, name, species, date_added):
		self.name = name
		self.species = species
		self.date_added = date_added
		self.slithering = True
		
kingsnake = Kingsnake('Kirby', 'kingsnake', date.today())

class Anaconda():
	def __init__(self, name, species, date_added):
		self.name = name
		self.species = species
		self.date_added = date_added
		self.slithering = True
		
anaconda = Anaconda('Aragorn', 'anaconda', date.today())

class Mallard():
	def __init__(self, name, species, date_added):
		self.name = name
		self.species = species
		self.date_added = date_added
		self.swimming = True
		
mallard = Mallard('Minerva McGonagall', 'mallard', date.today())

class Goldfish():
	def __init__(self, name, species, date_added):
		self.name = name
		self.species = species
		self.date_added = date_added
		self.swimming = True
		
goldfish = Goldfish('Godzilla', 'goldfish', date.today())

class Swan():
	def __init__(self, name, species, date_added):
		self.name = name
		self.species = species
		self.date_added = date_added
		self.swimming = True
		
swan = Swan('Severus Snape', 'swan', date.today())

class Catfish():
	def __init__(self, name, species, date_added):
		self.name = name
		self.species = species
		self.date_added = date_added
		self.swimming = True
		
catfish = Catfish('Cheshire Cat', 'catfish', date.today())

class Bass():
	def __init__(self, name, species, date_added):
		self.name = name
		self.species = species
		self.date_added = date_added
		self.swimming = True
		
bass = Bass('Beetlejuice', 'bass', date.today())
